
# Construction Quiz App

### Local dev
```bash
npm install
npm run dev
```
Open http://localhost:3000.

### Deploy to Vercel
Push the folder to GitHub or run `vercel` in the project directory.

Place your full `questions.json` in `/app/data/` and redeploy any time you update questions.
